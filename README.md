# booking-dashboard
Local Service Booking Dashboard is a simple frontend app (HTML/CSS/JS) that helps small businesses manage appointments using browser localStorage. Add, view, edit, delete, search bookings; change status (Upcoming/Completed/Cancelled); and see auto-updating stats. Mobile-first, no backend, teaches CRUD, DOM, and state handling.
